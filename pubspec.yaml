name: boring_table
description: A new Flutter package project.
version: 0.0.1
homepage: null

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dropdown_button2:
    git:
      url: git@github.com:BoringStuff-SRL/drop_down.git

  flutter_svg: ^2.0.10+1
  linked_scroll_controller: ^0.2.0
  flutter_slidable: ^3.1.0

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter: null

